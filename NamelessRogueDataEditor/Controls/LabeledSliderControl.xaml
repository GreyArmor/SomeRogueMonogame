<UserControl x:Class="NamelessRogueDataEditor.Controls.LabeledSliderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="400">
    <Grid>
        <StackPanel Margin="4">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           VerticalAlignment="Center" />
                <TextBlock Text=": " VerticalAlignment="Center" Margin="4,0,0,0"/>
                <ui:NumberBox VerticalAlignment="Center" Minimum="-999" Maximum="999" Width="60"
                           Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           Margin="4,0,0,0"/>
            </StackPanel>

            <Slider Value="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Minimum="-999"
                    Maximum="999"
                    Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
